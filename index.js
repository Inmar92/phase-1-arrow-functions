function divide() {
    let a = 2000;
    let b = 100;
    let divide = (a / b)
    return divide
}

const square = x => x + 2;
square;

const add = (parameter1, parameter2) => parameter1 + parameter2;
add(2,3);